server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /add {
        proxy_pass http://backend:5000/add;
    }

    location /users {
        proxy_pass http://backend:5000/users;
    }
}
